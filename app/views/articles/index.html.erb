<div class="navbar">
    <h1 class="top"><%= link_to "Wikinimous", articles_path %></h1>
    <h1 class="top"><%= link_to "New article", new_article_path %></h1>
</div>

<div class="container">
    <div class="article">
      <% @articles.each do |article| %>
      <%# link_to "#{task.title}", "tasks/#{task.id}" %>
      <h1 class="title"><%= link_to "#{article.title}", article_path(article) %></h1>
      <h1 class="content"><%= article.content %></h2>
      <% end%>
    </div>
</div>
